# printer.cfg — Cosmos-Nebula user configuration
# This file is yours to edit. It includes machine.cfg (hardware definitions)
# and macros.cfg (print macros), then you add your overrides and calibration here.

[include machine.cfg]
[include macros.cfg]

# Uncomment klicky.cfg when using the RP2040 bed MCU with Klicky probe:
#[include klicky.cfg]

# =============================================================================
# Add your calibration overrides, extra macros, or config below.
# These override values in machine.cfg.
# =============================================================================

# Example: override pressure advance after calibration
#[extruder]
#pressure_advance: 0.04

# Example: override Z offset after first layer tuning
#[probe]
#z_offset: 0.0
