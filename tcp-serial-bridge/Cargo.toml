[package]
name = "tcp-serial-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
serialport = { version = "4", default-features = false }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
